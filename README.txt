# Aziz Portfolio — Static (без сборки)

Откройте `index.html` двойным кликом — сайт сразу работает.
Чтобы поменять текст/контакты — правьте прямо в `index.html`.
Кнопка RU/EN переключает язык локально (JS без бэкенда).
